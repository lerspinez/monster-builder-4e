
<MudStack Row StretchItems="StretchItems.Start" AlignItems="AlignItems.End" @onclick="ExpandCollapse"
    Class="px-2 py-1 cursor-pointer" Style="background-color: lightseagreen">
    <MudText Typo="Typo.h6">@SectionTitle</MudText>
    <MudIcon Icon="@(_contentExpanded ? Icons.Material.Filled.ExpandLess : Icons.Material.Filled.ExpandMore)" />
</MudStack>

<MudCollapse Expanded="_contentExpanded">
    @ChildContent
</MudCollapse>

@code {
    [Parameter]
    public string SectionTitle { get; set; } = string.Empty;

    [Parameter]
    public RenderFragment? ChildContent { get; set; }

    bool _contentExpanded = true;

    private void ExpandCollapse()
    {
        _contentExpanded = !_contentExpanded;
    }

    public void Collapse()
    {
        _contentExpanded = false;
    }
}
